# Phase 09

| Was | Now | Statement | Evidence |
|---|---|---|---|
| unknown | known | within-project retrieval (square-okhttp, the only project with real class balance) scores precision@5 = 0.993 against a majority baseline of 0.585 -- a large, real improvement, with a healthy 4.64 mean distinct tests among the 5 neighbours (not repeats of one test) | artifacts/results/retrieval.json |
| unknown | known | cross-project retrieval is not performable with this data: kevinsawicki and tootallnate contribute zero not-flaky documents each, so holding either out produces a 100%-flaky query population where precision@k trivially equals the majority baseline (both exactly 1.0) -- not evidence of discrimination; the one informative direction (hold out square-okhttp, index the other two) cannot run because those two have no not-flaky documents to build a training index from | artifacts/results/retrieval.json |
| unknown | known | the per-method failure-detail header in maven.log uses a fully-qualified class name, while phase 05's deterministic-test naming convention (from the Results-block summary) uses the short class name -- without normalizing, deterministic-test failures were silently mislabeled "flaky" across an entire project | ai-ledger/09-retrieval-observer.md |
| unknown | known | documents are capped at 5 occurrences per test (`max_docs_per_test`) -- a deterministic test can fail near-identically in thousands of runs, and embedding every copy adds cost without diversity | ci_triage/retrieval.py |
| unknown | known-unknown | whether cross-project retrieval would actually work is unresolved -- only 1 of 3 available projects has any not-flaky documents at all, so this cannot be tested with the current raw-archive data regardless of method; it would need at least 2 projects with real not-flaky text, which none of the 3 archived projects jointly provide except square-okhttp alone |
| unknown | known-unknown | whether the strong within-project result (0.993) is specific to square-okhttp's failure-text vocabulary (dominated by one recurring JDK/SSL error) or would hold on a project with more varied failure causes is untested -- only one project had enough class balance to evaluate at all |
