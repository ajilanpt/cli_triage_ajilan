# Phase 05

| Was | Now | Statement | Evidence |
|---|---|---|---|
| unknown | known | square-okhttp's `maven.log` uses a different failing-test name format than kevinsawicki-http-request (`Class.method:line`, with `Outer>Inner.method:line` for inherited methods, vs. `method(Class):`); a name regex tuned to only one format silently returns zero matches on the other, producing a fabricated "0 failed" verdict instead of an error | ci_triage/infra.py, tests/test_infra.py::test_unrecognized_failing_test_format_is_unknown_not_silently_zero |
| unknown | known | the fix is a runtime cross-check: each `Results :` block's own `Failures + Errors` count is ground truth: if the parsed failing-test names don't add up to it, the run is reported `UNKNOWN` rather than a verdict built on an unverifiable parse | ci_triage/infra.py::parse_run_log |
| unknown | known | one broken `@Before`/`setUp` can fail several `@Test` methods under a single repeated name (e.g. `ConnectionPoolTest#setUp` x19 in one run); the mass-failure fraction must count each failed *execution* (undeduped), not each distinct name, or it undercounts | ci_triage/infra.py, tests/test_infra.py::test_repeated_name_counts_each_failed_execution_in_the_fraction |
| unknown | known | across all 7,908 square-okhttp runs, `BUILD FAILURE` never appears — this project's harness apparently never fails to compile in the archived data, matching the earlier kevinsawicki finding that Maven's own reactor status is not a usable trust signal (`testFailureIgnore`-style config hides test failures from it) | manual inspection, `data/raw/square-okhttp.tgz` |
| unknown | known | 85 tests fail in >=80% of square-okhttp runs (deterministic, not random); the dominant cause inspected by hand is a JDK/SSL API mismatch (`NoSuchMethodError: Handshaker.setSNIServerNames`), not flakiness — excluding these before scoring is what keeps the mass-failure fraction meaningful | artifacts/results/infra.json |
| unknown | known | at a 10% mass-failure threshold, exactly 1 of 7,908 square-okhttp runs (`2540`) is flagged `MASS_FAILURE`; its dominant cause, inspected by hand, is a real DNS resolution failure (`UnknownHostException ... Temporary failure in name resolution`) inside `URLConnectionTest.setUp`, cascading into every test that ran after it — a genuine infrastructure-poisoned run, not a code bug | artifacts/results/infra.json |
| unknown | known | excluding that one poisoned run changes the headline pair: distinct failing tests using all runs = 52, using only trusted runs = 44 — 8 tests that only ever "failed" inside the poisoned run disappear once it is excluded. The two earlier archives (kevinsawicki, tootallnate) showed no gap at all here because neither had any non-`TRUSTED` runs | artifacts/results/infra.json |
| unknown | known-unknown | whether discarding a run throws away real flakiness along with noise is not resolved by this evidence. Only 1/7,908 square-okhttp runs was excluded as `MASS_FAILURE`, so the practical cost looks small for this project specifically — but the 85 deterministically-excluded tests are removed from *every* run's scoring, and whether any of them are genuinely flaky rather than purely environment-broken was never checked against FlakeFlagger's own labels for those exact tests |
| unknown | known-unknown | the 10% mass-failure threshold was chosen from what this one project's corpus happened to produce (its worst non-deterministic run measured 10.07%); it has not been checked against kevinsawicki or tootallnate, and whether 10% generalizes to a project with a different scale or shape of infrastructure noise is untested |
